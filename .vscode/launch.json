{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "STEP 2: Attach Debugger",
      "request": "attach",
      "type": "dart",
      "vmServiceUri": "${command:dart.promptForVmServiceUri}"
    }
  ]
}
